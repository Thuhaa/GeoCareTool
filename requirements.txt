# GeoCareTool — runtime dependencies
# Pinned lower bounds only; let pip pick recent compatible versions.
# Install with:  pip install -r requirements.txt
# Or install the package directly: pip install -e .

# ── Data / arrays ──────────────────────────────────────────────
pandas>=1.5
numpy>=1.23

# ── Geospatial ─────────────────────────────────────────────────
geopandas>=0.13
shapely>=2.0            # required for shapely.concave_hull (isochrones)
osmnx>=1.6              # OSM walking network downloads
networkx>=3.0
geopy>=2.3

# ── Plotting / basemaps ────────────────────────────────────────
matplotlib>=3.6
contextily>=1.3         # basemap tiles for plot_grid_with_basemap

# ── HTTP / Google Places ───────────────────────────────────────
googlemaps>=4.10
requests>=2.28

# ── Misc ───────────────────────────────────────────────────────
unidecode>=1.3
python-dotenv>=1.0      # loads GOOGLE_API_KEY from .env

# ── Optional (uncomment to enable) ─────────────────────────────
# Public-transport travel-time matrices (needs Java 11+ for R5)
# r5py>=0.1.0
